name: Legacy V65 matchmaking patch (disabled)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy V65 patch disabled; current V69 pipeline owns live organization deployment.'
