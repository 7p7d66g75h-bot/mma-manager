name: Legacy V66 refusal batch patch (disabled)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy V66 patch disabled; current V69 pipeline owns live organization deployment.'
