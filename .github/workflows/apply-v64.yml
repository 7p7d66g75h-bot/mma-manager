name: Legacy V64 organization patch (disabled)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy V64 patch disabled; current V69 pipeline owns live organization deployment.'
