name: Apply V62 gameplay stability fix
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "V62 auto patch disabled; matchmaking rollback in progress"